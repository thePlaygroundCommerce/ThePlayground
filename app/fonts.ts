import { Lato } from "next/font/google";

export const lato400 = Lato({
  weight: "700",
  style: ["normal", "italic"],
  subsets: ["latin"],
  display: "swap",
});

export const lato900 = Lato({
  weight: "900",
  style: ["normal", "italic"],
  subsets: ["latin"],
  display: "swap",
});
